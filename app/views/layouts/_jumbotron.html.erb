<% if !user_signed_in? %>
  <div class=" jumbotron">
    <div class="container-fluid align-content-center">
      <h1>GamersBuff</h1>
      <p>Accept the challenge , join the competition</p>
      <%= link_to 'Sign up now!', new_user_registration_path, class: 'btn btn-lg btn-primary' %>
    </div>
  </div>
<% end %>
