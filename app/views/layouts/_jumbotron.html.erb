<% if !user_signed_in? %>
  <div class="jumbotron mb-0">
    <div class="container-fluid align-content-center">
      <h1>GamersBuff</h1>
      <p><%= t('jumbotron.challenge') %></p>
      <%= link_to t('jumbotron.signup'), new_user_registration_path, class: 'btn btn-lg btn-primary' %>
    </div>
  </div>
<% end %>
